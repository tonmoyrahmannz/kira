# 2026-05-15

- Home Assistant migration update: HAOS moved to dedicated Wyse 5070 bare-metal appliance at `192.168.50.166` (`generic-x86-64`, healthy/supported).
- Mac Mini (`192.168.50.208`) no longer hosts HA; it remains Plex/NAS/Tailscale subnet router/aux services.
- Updated Kira config, scripts, runbooks, system map, and staging dashboard topology to remove VMware/HA-VM assumptions.
- Migration state: bare-metal install complete; backup restore still pending.
- Patched MealPrep scripts toward Mac Mini portability: reminders flow now prefers local `osascript`, wrapper paths are dynamic, and HA sync script no longer depends on local GNU-only `sha256sum`/`stat -c`/`find -printf`/`sort -z` behavior.
