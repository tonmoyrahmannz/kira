# 2026-03-16

- Built and iterated heavily on Home Assistant dashboards for Rahman Command Centre, especially God’s Eye and TEST_NOC_2 topology card.
- Final topology hierarchy requirements clarified and enforced:
  - Internet -> Router/Gateway
  - Mac Mini -> VMware Fusion -> HAOS VM (HA CORE)
  - HAOS VM children include Tuya Platform subtree (Tuya Cloud, Tuya Zigbee Gateway, LocalTuya LAN Control), Gree HVAC, Neo Blinds
  - Mac Mini also hosts Plex
  - Omarchy hosts OpenClaw/Kira runtime
- Fixed pulse behavior in TEST_NOC_2 topology so pulse only appears for non-healthy nodes; healthy nodes stay static.
- Resolved false Mac Mini offline logic by using available entities (`binary_sensor.nas_online` and `sensor.macmini_cpu_usage`) instead of missing `binary_sensor.macmini_online`.
- Added direct HA file transfer capability confirmed by user (important operational change): can now SCP files directly to HA `/config` from this environment.
- Implemented Tailnet-wide Tailscale monitoring package in HA (`tailscale_tailnet_health.yaml`) using Tailscale API in HA REST sensor (no OpenClaw cron).
- Updated Tailscale polling interval to every 5 minutes (300s) as requested.
- Added named device mapping by Tailscale IP suffix:
  - `*.69` => Tonmoy’s iPhone
  - `*.29` => Esha’s iPhone
- Added HA entities for per-device online and last-seen state:
  - `binary_sensor.tailscale_tonmoy_iphone_online`
  - `binary_sensor.tailscale_esha_iphone_online`
  - `sensor.tailscale_tonmoy_iphone_last_seen`
  - `sensor.tailscale_esha_iphone_last_seen`
- Verified after reload: tailnet sensors active and reporting 4 total/4 online in current snapshot.
