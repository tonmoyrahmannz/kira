# 2026-05-17

- Created a Home Assistant dashboard copy `ac-blinds-no-expander` titled `AC and Blinds - No Expander`, copied from `test-2` / `AC and Blinds`. Converted all 4 `custom:expander-card` wrappers into normal `vertical-stack` sections in the copy only. Verified source still has 4 expander cards and the copy has 0.
- Updated Home Assistant `new-mobile-dashboard` Home Climate Controls card to show humidity above each room climate tile. Added compact humidity cards for Living Room, Master Bedroom, Study, and Guest Bedroom using `sensor.living_room_sensor_humidity`, `sensor.master_bedroom_humidity`, `sensor.study_sensor_humidity`, and `sensor.guest_sensor_humidity`. Verified dashboard loads and contains 4 Kira humidity badges.
- Built Morning Briefing Source Pull tooling for the Mac Mini: `scripts/morning_briefing_source_pull.py`, `scripts/morning-briefing-source-pull.sh`, and `skills/apple-shortcuts-morning-briefing/SKILL.md`. It writes `state/morning_briefing_sources.json` and `state/morning_briefing_sources.md`, pulling Home Assistant, local Reminders, local Calendar where accessible, and Shortcuts CLI status. Current limitation: local Calendar AppleScript times out; Google Calendar connector successfully returned upcoming event `Stay at Zen hideaway in Aotea`.
- Repaired Kira's browser automation path on the Mac Mini by configuring a direct Chrome DevTools profile (`chrome-user-cdp`) in OpenClaw and fixing the local pairing state that was blocking browser commands. Verified the managed-browser flow can launch Chrome, open a page, and return a DOM snapshot; the direct CDP route is the practical option for using the user's signed-in Chrome session.
- Clarified that Kira does not need the Chrome extension for the current browsing setup when using the direct Chrome CDP profile. The extension may still be useful for other workflows later, but it is not required for basic browser control on this machine.
- User asked about sourcing a replacement bonnet/hood for a blue 2007 Mazda Demio hatchback. A parts search was started across wreckers; keep the exact vehicle details handy for follow-up lookups.
