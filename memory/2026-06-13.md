# 2026-06-13

## Memory policy correction
- Removed volatile configured/active model details from `MEMORY.md`.
- Runtime and model observations belong in `docs/session_state.md` or a dedicated runtime status file; `MEMORY.md` is reserved for durable facts.

## Tailscale subnet router migration
- Tailscale subnet router moved from Mac Mini to the HAOS thin client (Wyse 5070)
- Installed official Tailscale add-on in HA via Settings → Apps
- Configured `advertise_routes: ["192.168.50.0/24"]` and approved in Tailscale admin console
- Mac Mini no longer advertises subnets ("Use Tailscale subnets" unchecked)
- Tonmoy's new iPhone 17 (`iphone173`/`iphone183` on tailnet) can now reach home devices via VPN
- Old iPhone 13 Pro Max still on tailnet as backup device
- HA thin client: `homeassistant.taila9375b.ts.net` / `100.90.241.109`
- Tailscale add-on version checked: 0.28.1 (up to date)
- HA Tailscale integration (HACS) shows 6 devices, 4 online

## Updated documentation
- MEMORY.md — updated subnet router info, added 2026-06-13 entry
- SYSTEM_MAP.md — bumped date, moved Tailscale subnet router under HAOS thin client
- memory/2026-05-15.md — corrected Mac Mini role (no longer subnet router)
- docs/runbooks/Smart_Home_Runbook_v8.md — updated failure scenario C and recovery procedure to reference HAOS thin client

## Kira workspace consolidation
- Moved NAS unique content into `~/Kira/`:
  - `Context/` from NAS → `~/Kira/Context/` (Career, Garden, Whiro Analytics, Household Finance, Personal, Principles, Home Automation)
  - `Tools/` from NAS → `~/Kira/Tools/` (household budget planner)
- Removed 6 duplicated files from NAS (Runbooks, Reference, Agent_Notes) — canonical versions now in `~/Kira/docs/`
- Updated AGENTS.md with:
  - Step 9 in startup sequence (optionally skim Context/ by session topic)
  - Workspace Layout section showing full tree
- NAS Kira directory fully removed

## MealPrep consolidated into ~/Kira/
- Moved entire NAS `Documents/MealPrep/` → `~/Kira/mealprep/`
  - Meal_Plan.md, Shopping_List.md, Recipes/ (8 recipes), config/, inventory/
  - Trimmed .backups from thousands of old backups to latest 10
- Updated all script defaults (`reminders_to_nas_shopping_sync.sh`, `sync_mealprep_to_ha.sh`, `run_mealprep_flow.sh`):
  - `NAS_ROOT` default changed from `~/nas` → `~/Kira`
  - `NAS_MEALPREP_DIR`/`SRC_BASE` default changed from `~/nas/Documents/MealPrep` → `~/Kira/mealprep`
- Updated `SKILL.md` and `references/flow-reference.md` to reflect new paths and removed NAS terminology
- NAS `Documents/MealPrep/` removed entirely
- NAS `Documents/` now only contains: Projects, Sketchup, Work
- Confirmed durable policy: `/Users/tonmoyrahman/Kira/mealprep/` is the MealPrep source of truth; older NAS paths under `Documents/MealPrep/` are historical unless explicitly restored.

## Directory naming convention applied
- All directories lowercase with hyphens (kebab-case):
  - `Context/` → `context/` (subdirs: `career/`, `garden/`, `home-automation/`, `household-finance/`, `personal/`, `principles/`, `whiro-analytics/`)
  - `Tools/` → `tools/`
  - `ha_config_staging/` → `ha-config-staging/`
  - `mealprep/Recipes/` → `mealprep/recipes/`
- Updated AGENTS.md layout, Tuya IR runbook paths, fix_dashboard_placeholders.py
- Identity files (SOUL.md, AGENTS.md, etc.) kept UPPERCASE as convention
