# 2026-05-16

- Investigated missing Home Assistant backup location after HAOS migration to the Wyse 5070 host.
- Supervisor had lost all configured mounts after migration (`/mounts` returned no mounts and `default_backup_mount: null`).
- Recreated Supervisor CIFS backup mount `plex_hd_backups_homeassistant` to Mac Mini NAS `192.168.50.208` share `Plex_HD/Backups/HomeAssistant` using the existing SMB credentials.
- Set `plex_hd_backups_homeassistant` as the default Home Assistant backup mount and confirmed the mount state is `active`.
- Reloaded Supervisor backups and confirmed remote backup visibility returned: `/backups/info` again shows `plex_hd_backups_homeassistant` in backup `locations`, including historical remote backups.
- Attempted a fresh verification backup via Supervisor API; the API returned `unknown_error`, but remote backup location discovery and mount state were restored successfully.
- On the Mac Mini, VMware Fusion.app was already absent from `/Applications`. Cleaned up remaining user-level VMware Fusion support files by moving them to `~/.Trash`, while intentionally preserving the VM bundle `~/Virtual Machines.localized/Other Linux 6.x kernel 64-bit Arm.vmwarevm`.
- Later moved the preserved VMware VM bundle itself to the Mac Mini Trash as well.
- Ran a Mac Mini performance cleanup pass: stopped Safari/Word plus removable background apps, moved Chrome/Spotify/uTorrent and substantial related user data/caches (including ChaosGroup, Google Chrome data, Spotify data, OpenAI Atlas cache, and OneDrive user data) to Trash. Root-owned remnants still require interactive `sudo` on the Mac Mini to fully remove/disable OneDrive and Logitech system components.
- Verified active Kira/OpenClaw Telegram runtime is now on the Mac Mini (`Macmini`, macOS 26.3, workspace `/Users/tonmoyrahman/Kira`) rather than Omarchy. Patched durable migration state in `SYSTEM_MAP.md`, `MEMORY.md`, `docs/session_state.md`, Kira setup docs, and the runbook builder so Mac Mini is the current source of truth and Omarchy is treated as legacy/secondary.
- Verified OpenClaw macOS LaunchAgent `ai.openclaw.gateway` uses Homebrew OpenClaw on port `18789`, and `~/.openclaw/openclaw.json` now points agent workspace to `/Users/tonmoyrahman/Kira`.
- Paused MealPrep by adding a pause guard to `skills/mealprep-flow/scripts/run_mealprep_flow.sh` and writing `~/nas/Documents/MealPrep/.status/paused`. Cleared all 38 reminders from Apple Reminders list `Family Shopping` and verified the list is empty. Also corrected `~/nas` from a local stub directory to a symlink to `/Volumes/Plex_HD`.
- Checked Omarchy laptop (`TonmoysLaptop`) for accidental OpenClaw Gateway restart risk. Found user-level `openclaw-gateway.service` enabled but inactive. Disabled autostart, preserved timestamped disabled service copies, replaced the user unit with a `/dev/null` mask, verified `is-enabled=masked`, `is-active=inactive`, no default.target wants symlink, no gateway process, and no listener on port `18789`. Left `kira-ha-telemetry.timer` active.
- User clarified the canonical v5 professional smart-home documentation lives at `/Volumes/Plex_HD/Backups/HomeAssistant/Smart_Home_Technical_Documentation_v5_Professional.md`. Updated that NAS file in place to reflect Mac Mini as the primary Kira/OpenClaw host, Wyse 5070 as Home Assistant host, Omarchy as one-week legacy fallback with gateway masked, NAS paths on `/Volumes/Plex_HD`, and MealPrep paused. Moved the earlier workspace-created copy to Trash to avoid duplicate sources of truth.

## Pre-Compaction Snapshot - 14:45 NZT

- Current stable setup: Kira/OpenClaw main direct Telegram session is operating from the Mac Mini, with the OpenClaw gateway managed by macOS LaunchAgent on port `18789`.
- Omarchy/laptop should no longer start OpenClaw gateway accidentally: user systemd gateway unit is masked and inactive; keep Omarchy only as a short-term one-week fallback before full removal.
- Smart-home documentation source of truth for the professional v5 doc is the NAS copy at `/Volumes/Plex_HD/Backups/HomeAssistant/Smart_Home_Technical_Documentation_v5_Professional.md`.
- Home Assistant is now on the dedicated Wyse 5070 HAOS host at `192.168.50.166`; Mac Mini remains Plex/NAS/Tailscale/aux services.
- MealPrep remains paused via NAS pause marker; do not repopulate `Family Shopping` unless user resumes MealPrep or explicitly asks.
- If continuing the docs task after compaction, verify the NAS smart-home doc diff/content directly before reporting final status; do not touch workspace reference files during memory flushes.
